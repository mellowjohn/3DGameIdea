# TICKET-0277: Authored mesh LOD + landmark impostor swap

- Epic: EPIC-0013
- Status: needs-approval
- Agent: cursor-agent
- Priority: P0
- Notion: (repo-first; mirror when Notion sync runs)

## Goal

Replace far-cull-only placed-mesh distance handling with **optional authored LOD1/LOD2 mesh swaps** (and a cheap landmark far stand-in) so hubs and vistas keep readable silhouettes without drawing full glTF at mid/far range.

## Context links

- [`../features/streaming-lod-budgets.md`](../../features/streaming-lod-budgets.md) — distance bands; impostors still design intent
- [`../formats/prefab-assets.md`](../../formats/prefab-assets.md) — prefab schema
- `include/engine/rendering/mesh_distance_lod.h` — LOD constants + `select_level`
- Soft: TICKET-0220 (far cull), TICKET-0276 / 0278 (GPU foliage cull)

## Acceptance criteria

- [x] Prefab schema supports optional `lod1Mesh` / `lod2Mesh` (project-relative glTF paths); missing fields keep current full-mesh + far-cull behavior
- [x] `required_mesh_keys` / catalog mesh ensure includes LOD meshes so they upload without restart
- [x] Placed prop draw path selects LOD0 / LOD1 / LOD2 / cull by camera distance with hysteresis (no flicker at band edges)
- [x] Distance constants live in `mesh_distance_lod.h` and are documented in streaming-lod-budgets
- [x] At least one sample prefab (`oak_tall`) authors a cheaper LOD1 mesh path for verification
- [x] Named suite asserts band ordering (LOD1 start < LOD2 start < far cull)
- [x] Feature / format docs updated; RelWithDebInfo `engine benchmark` still exits 0

## Out of scope

- Autogenerated mesh simplification / Nanite
- Billboard impostor atlases / card baking (follow-on)
- Foliage blade LOD (scatter falloff already exists)
- Prop GPU cull / ExecuteIndirect (follow-on after TICKET-0278 re-enable)
- Requiring every prefab to author LODs

## Dependencies

Soft after TICKET-0220. Parallel OK with TICKET-0278.

## Verification

- RelWithDebInfo rebuild `engine` + `engine_suite_tests`: OK
- `engine_suite_tests --suite assets` → 97/97 (includes LOD band + select_level checks)
- `engine validate --project samples/open-world-rpg` → exit 0
- RelWithDebInfo `engine benchmark` → ~3.5–3.9 ms CPU / ~1.0 ms GPU on 2080 SUPER

## What changed

- Summary: Prefabs can declare optional mid/far meshes; the prop draw path swaps LOD0→LOD1→LOD2→cull with hysteresis. `oak_tall` uses `oak_young` as LOD1/LOD2 stand-in. Missing LOD fields preserve the old far-cull-only behavior.
- Files / surfaces touched: `prefab_asset.h/.cpp`, `mesh_distance_lod.h`, `render_app.cpp` (expand + `draw_prop_instances`), `oak_tall.prefab.json`, suite tests, streaming-lod / prefab format / features index docs.
- Schema / API / format deltas: optional `lod1Mesh` / `lod2Mesh` on prefab JSON.
- Seed / sample data: `oak_tall` LOD paths only; other oaks unchanged.
- Tests / verification evidence: assets suite 97/97; validate + RelWithDebInfo benchmark exit 0.
- Decisions & tradeoffs: per-prefab mesh swap (not per-part); billboard impostors deferred.
- Leftover risk / follow-ons: true low-poly LOD meshes / impostor atlases; CSM still draws full mesh.

## Agent notes

Landed with TICKET-0278 ring work in the same pass; GPU cull draw-path remains off.
